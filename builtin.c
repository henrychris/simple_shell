#include "shell.h"

/**
 * env - get the environment
 * Return: Always succeeds
 */
int env(void)
{
	char **env = environ;

	while (*env)
	{
		write(1, *env, strlen(*env));
		write(1, "\n", 1);
		env++;
	}
	return (0);
}
